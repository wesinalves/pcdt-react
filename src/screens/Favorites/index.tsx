import React from "react";
import { Container } from "../Favorites/styles";
import { Text } from "react-native";

export default () => {
    return (
        <Container>
            <Text>Favorites</Text>
        </Container>
    );
}
